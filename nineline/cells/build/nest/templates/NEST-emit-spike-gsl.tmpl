	if ( S_.r_ > 0 )
	   S_.r_--;
	else if (S_.y_[{{stateIndexMap["v"]}}] >= P_.V_t &&
	    V_.U_old_ > S_.y_[{{stateIndexMap["v"]}}]) {
	    S_.r_ = V_.RefractoryCounts_;
		set_spiketime(Time::step(origin.get_steps()+lag+1));
	   	SpikeEvent se;
		network()->send(*this, se, lag);
	}