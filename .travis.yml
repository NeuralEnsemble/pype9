language: python
python:
  - "2.7"
sudo: required
dist: trusty
addons:
  apt:
    packages:
      - build-essential
      - autoconf
      - automake
      - libtool
      - libreadline6-dev
      - libncurses5-dev
      - libgsl0-dev
      - python-all-dev
      - python-numpy
      - python-scipy
      - openmpi-bin
      - libopenmpi-dev
install:
  - pip install cython
  - source prereq/install_nest.sh
  - source prereq/install_neuron.sh
  - pip install https://github.com/sympy/sympy/archive/master.zip
  - pip install -r requirements.txt
  - pip install https://github.com/tclose/lib9ML/archive/develop.zip
  - pip install https://github.com/tclose/NineMLCatalog/archive/merging_with_master.zip  
  - pip install coveralls
  - pip install .
script:
  - ls /home/travis/build/CNS-OIST/PyPe9/pype9/neuron/cells/code_gen/libninemlnrn
  - nosetests test --exe --with-coverage --cover-package=pype9
after_success:
  - coveralls
cache:
  directories:
    - $HOME/nest-2.10.0
    - $HOME/nrn-7.4
    - $HOME/build/nest-2.10.0
    - $HOME/build/nrn-7.4
    - $HOME/.cache/pip
